{% extends 'base.html.twig' %}

{% block title %}
  Ma récompense
{% endblock %}

{% block body %}
  <div id="scroll-point" class="my-4">{{ '<br>'|raw }}</div>
  <section class="features-section mb-4">
    <div class="container text-center mx-auto">
      <h3 class="section-subtitle mb-3">{{ reward.title }}</h3>
      <p class="text-secondary mb-1">bla bla sur les récompenses gagnées ...</p>
      <div class="justify-content-center">
        <div class="col-10 col-lg-6">
          <img src="{{ vich_uploader_asset(reward, 'pictureFile') }}" alt="{{ reward.title }}" />
        </div>
      </div>
    </div>
  </section>
{% endblock %}
